# SOURCE INVENTORY

This is a map of what the supplied reference actually contains.

## Complete implementations present

### WebGL Liquid
- full TypeScript/React component source,
- vertex shader,
- fragment shader,
- prop API,
- WebGL setup/resize/render/cleanup,
- fallback rendering structure.

### DepthCarousel
- full TypeScript/React component source,
- GSAP navigation,
- pointer drag,
- wheel,
- keyboard,
- autoplay,
- responsive scaling,
- accessibility structure,
- prop API.

### BorderGlow
- full TypeScript/React component source,
- pointer edge proximity,
- cursor-angle tracking,
- mesh gradients,
- masked fill,
- outer glow,
- intro sweep,
- prop API.
- The same component is repeated multiple times in the supplied notes; treat it as one implementation.

### GradientWaves
- full TypeScript/React component source,
- OGL/WebGL2 setup,
- GLSL vertex + fragment shader,
- raymarch/plasma field,
- mouse parallax,
- visibility-aware loop,
- page visibility handling,
- prop API.

## Partial implementation only

### Scroll Split Card
The supplied notes provide:

- installation command,
- usage example,
- 3 panel data shape,
- conceptual description,
- `animejs` installation note.

They do not provide the full component source.

## Source-derived placement notes

- `WebGL Liquid` is explicitly marked as the hero.
- `DepthCarousel` is explicitly marked as the effect used for showing agents.
- `BorderGlow` is marked as reusable wherever necessary.
- `GradientWaves` is explicitly marked for a section.
- `Scroll Split Card` is explicitly marked as a scroll-driven section.
